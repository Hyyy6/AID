<!DOCTYPE html>
<html lang="en">
<link rel="stylesheet" href="static/auth.css" />
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
</head>
<body>
    <div class="container">
        <div class="page-info">
            <!-- Add content for page info or remove if not needed -->
        </div>
        <div class="form-container">
            <h2>Login</h2>
            <form id="login-form" method="post" action="{{ url_for('auth.login') }}">
                <div class="form-group">
                    <label for="username">Username</label>
                    <input type="text" id="username" name="username" required>
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required>
                </div>
                <button type="submit">Login</button>
            </form>
            <div class="alternative-login">
                <p>Or login with:</p>
                <!-- Add buttons or links for alternative login methods (e.g., Google, Microsoft) -->
            </div>
            <p>Don't have an account? <a href="{{ url_for('auth.signup') }}">Signup</a></p>
        </div>
    </div>
</body>
</html>
